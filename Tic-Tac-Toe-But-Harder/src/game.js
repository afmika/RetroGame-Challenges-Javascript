/**
 * @param {number} x 
 * @param {number} y 
 * @returns 
 */
function getGameCoordinates (x, y) {
    return [Math.floor (x / BLOC), Math.floor ( y / BLOC)];
}

/**
 * @param {Board} board 
 */
function getWinner (board) {
}